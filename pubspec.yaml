name: moor_views
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  moor: ^4.3.2
  sqlite3_flutter_libs:
  path_provider:
  path:

dev_dependencies:
  moor_generator:
  build_runner:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true